#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 on success
 */
int main(void)
{
	int number;

	for (number = 0; number <= 9; number++)
		printf("%i", number);
	printf("\n");
	return (0);
}
