#include "main.h"
/**
 * mul - function to multiply two integers
 * @a: first integer
 * @b: second integer
 * Return: answer of the multiplication
 */
int mul(int a, int b)
{
	int answer;

	answer = a * b;
	return (answer);
}
