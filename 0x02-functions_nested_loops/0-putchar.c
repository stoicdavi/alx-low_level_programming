#include "main.h"

/**
 * main - this is the entry point to the program
 *
 * Return: 0 every time the program runs succeesfuly
 */
int main(void)
{
	char name[] = "_putchar\n";
	int b;

	for (b = 0; b < 9; b++)
		putchar(name[b]);
	return (0);
}
