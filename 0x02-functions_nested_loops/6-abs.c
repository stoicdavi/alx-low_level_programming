#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 * @a: the interger to compute its absolute value.
 *
 * Return: absValue.
 *
 */
int _abs(int a)
{
	int absvalue;

	absvalue = abs(a);
	return (absvalue);
}
