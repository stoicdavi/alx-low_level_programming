#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char alphabet;

	for (alphabet = 97; alphabet <= 122; alphabet++)
		_putchar(alphabet);
	_putchar('\n');
	return (0);
}
